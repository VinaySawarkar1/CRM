# Environment Variables for Reckonix Management System
# Copy this file to .env.local for local development
# Set these variables in your Render dashboard for production

# Application Environment
NODE_ENV=production
PORT=10000
HOST=0.0.0.0

# Session Security
SESSION_SECRET=your-super-secret-session-key-here

# Database Configuration
USE_MONGODB=true
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/business_ai?retryWrites=true&w=majority
MONGODB_PASSWORD=your-mongodb-password
DB_NAME=business_ai

# Instructions:
# 1. Replace 'username' and 'password' in MONGODB_URI with your actual MongoDB credentials
# 2. Replace 'cluster' with your actual MongoDB cluster name
# 3. Generate a strong SESSION_SECRET (you can use: openssl rand -base64 32)
# 4. For Render deployment, set these as environment variables in your Render dashboard
